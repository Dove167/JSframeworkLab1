=== PUT full update ===
curl -i -X PUT http://localhost:3000/api/expenses/1 \
  -H 'Content-Type: application/json' \
  -d '{"title":"Groceries (weekly)","amount":40}'

=== PUT Response ===
(No output received - there were PowerShell quoting issues)

=== PATCH partial update (title only) ===
curl -i -X PATCH http://localhost:3000/api/expenses/1 \
  -H 'Content-Type: application/json' \
  -d '{"title":"Coffee beans"}'

=== PATCH Response ===
(No output received - there were PowerShell quoting issues)

=== PATCH partial update (amount only) ===
curl -i -X PATCH http://localhost:3000/api/expenses/1 \
  -H 'Content-Type: application/json' \
  -d '{"amount":99}'

=== PATCH Response ===
(No output received - there were PowerShell quoting issues)

=== Invalid PATCH (empty body) ===
curl -i -X PATCH http://localhost:3000/api/expenses/1 \
  -H 'Content-Type: application/json' \
  -d '{}'

=== Invalid PATCH Response ===
(No output received - there were PowerShell quoting issues)

=== PUT non-existent id ===
curl -i -X PUT http://localhost:3000/api/expenses/999 \
  -H 'Content-Type: application/json' \
  -d '{"title":"X","amount":1}'

=== PUT non-existent Response ===
(No output received - there were PowerShell quoting issues)